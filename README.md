# localMind
Projet pour decouvrir comment les llm interagissent avec leur environement. 


Le but est de faire un llm local (via ollaman) qui interragie avec un  
un autre systeme (volume sur une machine hote, via un docker python).  

Si vous souhaitez observer le resultat voici les commandes a mettre en place.  

```
git clone git@github.com:Cpt-Graby/localMind.git
cd localMind
```

## Lancement pour docker
```
```

## Lancement pour podman
```
podman compose up --build -d
docker attach localmind_agent
```
